% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom.R
\name{geom_fraction_bars}
\alias{geom_fraction_bars}
\title{Create Fraction-Based Area Plots with Periodic Highlighting}
\usage{
geom_fraction_bars(
  mapping = NULL,
  data = NULL,
  position = "identity",
  na.rm = FALSE,
  show.legend = FALSE,
  alpha = 0.3,
  period = 5,
  initial = 0L,
  lines = NA,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2]{aes}}. If specified and
\code{inherit.aes = TRUE}, is combined with the default mapping at the top level of the plot.}

\item{data}{The data to be displayed in this layer. If \code{NULL}, the default, the data
is inherited from the plot data as specified in the call to \code{\link[ggplot2]{ggplot}}.}

\item{position}{Position adjustment, either as a string, or the result of a call to a
position adjustment function. Default is "identity".}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a warning.
If \code{TRUE}, missing values are silently removed.}

\item{show.legend}{logical. Should this layer be included in the legends?
\code{NA}, the default, includes if any aesthetics are mapped.}

\item{alpha}{Numeric value between 0 and 1 for the transparency of the area fills.
Default is 0.3.}

\item{period}{Integer specifying the interval for highlighting fractions. Default is 5.}

\item{initial}{Integer specifying the starting point for fraction counting. Default is 0.}

\item{lines}{Vector of line colors to include in the visualization. Default is NA.}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics,
rather than combining with them.}

\item{...}{Other arguments passed to \code{\link[ggplot2]{layer}}.}
}
\value{
A ggplot2 layer.
}
\description{
Creates filled area plots that visually highlight periodic patterns in fractional data.
The highlighting is controlled by the period parameter and can be filtered by line colors.
}
\examples{
\dontrun{
ggplot(data, aes(x = x, y = y, fraction = frac)) +
  geom_fraction_bars(period = 5)

# With custom period and initial value
ggplot(data, aes(x = x, y = y, fraction = frac)) +
  geom_fraction_bars(period = 3, initial = 1, alpha = 0.2)
}
}
