% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{pivot_wl_longer}
\alias{pivot_wl_longer}
\title{Pivot Wavelength Data to Long Format}
\usage{
pivot_wl_longer(data, values_to = "abs", names_to = "wl")
}
\arguments{
\item{data}{A data frame containing wavelength measurements in wide format}

\item{values_to}{Name of the column to create for the values (default: "abs")}

\item{names_to}{Name of the column to create for the wavelength identifiers (default: "wl")}
}
\value{
A data frame in long format with wavelength measurements
}
\description{
Converts wavelength data from wide to long format, specifically handling
columns that match the pattern "(a|A)\\d{2,3}" (e.g., "A280", "a340").
}
\examples{
\dontrun{
# With default column names
pivot_wl_longer(spectral_data)

# With custom column names
pivot_wl_longer(spectral_data, values_to = "absorbance", names_to = "wavelength")
}
}
