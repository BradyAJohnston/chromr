% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom.R
\name{geom_fraction_label}
\alias{geom_fraction_label}
\title{Create Labels for Fractional Data}
\usage{
geom_fraction_label(
  mapping = NULL,
  data = NULL,
  position = "identity",
  na.rm = FALSE,
  show.legend = NA,
  xpos = mean,
  ypos = 0,
  period = 5,
  initial = 0L,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2]{aes}}.}

\item{data}{The data to be displayed in this layer.}

\item{position}{Position adjustment, either as a string, or the result of a call to a
position adjustment function. Default is "identity".}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a warning.}

\item{show.legend}{logical. Should this layer be included in the legends?}

\item{xpos}{Function or static value determining x position of labels. Default is mean.}

\item{ypos}{Function or static value determining y position of labels. Default is 0.}

\item{period}{Integer specifying the interval between labels. Default is 5.}

\item{initial}{Integer specifying the starting point for fraction counting. Default is 0.}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}

\item{...}{Other arguments passed to \code{\link[ggplot2]{layer}}.}
}
\value{
A ggplot2 layer.
}
\description{
Adds labels to plots at specified fractional intervals. Labels can be positioned
using either static values or functions for both x and y coordinates.
}
\examples{
\dontrun{
# Basic usage with default positioning
ggplot(data, aes(x = x, y = y, fraction = frac)) +
  geom_fraction_label()

# Custom positioning using functions
ggplot(data, aes(x = x, y = y, fraction = frac)) +
  geom_fraction_label(xpos = mean, ypos = max)
}
}
